import React, { Component } from 'react';

class PhotoContainer extends Component {
    render() {
        return (
            <h2>Container exists.</h2>
        );
    }
}

export default PhotoContainer;